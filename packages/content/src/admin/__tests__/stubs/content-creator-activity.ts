// Browser-test stand-in for `../../modules/content/models/content-creator-activity.ts` -- see
// `./content-collection.ts` for why. `validators.ts` only needs `ContentCreatorActivityType`.
export enum ContentCreatorActivityType {
	EDIT = 'edit',
	NOTE = 'note'
}
